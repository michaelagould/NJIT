#include <stdlib.h>

int* split(int* sample, int l, int r){
	if (sizeof(sample[0]) == sizeof(sample))
		return sample;
	else{
		leftIndex = 
		rightIndex
		middle = samplesize/2
		split(sample, )
		split(sample/(middle+1))
		merge(part1, part2)
	}

int main() {
	 
}