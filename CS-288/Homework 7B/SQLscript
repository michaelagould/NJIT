DROP DATABASE IF EXISTS all;

CREATE DATABASE all;

USE all

CREATE TABLE weather(
    state  CHAR(2),
    city VARCHAR(64),
    weather VARCHAR(32),
    temperature VARCHAR(3),
    humidity VARCHAR(3),
    pressure VARCHAR(5),

    PRIMARY KEY (state)
);