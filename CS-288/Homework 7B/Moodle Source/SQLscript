DROP DATABASE IF EXISTS weather;

CREATE DATABASE weather;

USE weather

CREATE TABLE data(
    state  CHAR(2),
    city VARCHAR(64),
    weather VARCHAR(32),
    temperature VARCHAR(3),
    humidity VARCHAR(3),
    pressure VARCHAR(5),

    PRIMARY KEY (state)
);